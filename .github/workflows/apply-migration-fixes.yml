name: Apply migration fixes
on:
  pull_request:
    branches: [main]
    types: [synchronize, reopened, ready_for_review]
permissions:
  contents: write
jobs:
  apply:
    if: github.head_ref == 'feat/providers-drizzle-phase12'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - run: |
          set -euo pipefail
          python3 .bootstrap/phase12/fix_migration.py
          rm -rf .bootstrap
          rm -f .github/workflows/apply-migration-fixes.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: complete legacy migration backfills and indexes'
          git push origin HEAD:${{ github.head_ref }}
